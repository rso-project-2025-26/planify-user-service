apiVersion: v1
kind: Secret
metadata:
    name: user-service-secrets
    namespace: planify
type: Opaque
stringData:
    # Database credentials
    DB_USERNAME: "planify"
    DB_PASSWORD: "CHANGE_ME_IN_PRODUCTION"

    # Keycloak admin credentials
    KEYCLOAK_ADMIN_USERNAME: "admin"
    KEYCLOAK_ADMIN_PASSWORD: "CHANGE_ME_IN_PRODUCTION"

---
# Example of using external secret management (sealed-secrets, external-secrets-operator, etc.)
# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: user-service-secrets
#   namespace: planify
# spec:
#   refreshInterval: 1h
#   secretStoreRef:
#     name: azure-key-vault
#     kind: SecretStore
#   target:
#     name: user-service-secrets
#     creationPolicy: Owner
#   data:
#     - secretKey: DB_PASSWORD
#       remoteRef:
#         key: planify-db-password
#     - secretKey: KEYCLOAK_ADMIN_PASSWORD
#       remoteRef:
#         key: planify-keycloak-admin-password
